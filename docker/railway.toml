# Railway deployment configuration for SPSA worker
# Deploy this service from the docker/ directory

[build]
dockerfilePath = "Dockerfile"

[deploy]
# No health check needed - this is a worker, not a web service
healthcheckPath = ""
# Restart on failure
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
