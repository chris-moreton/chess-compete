# SPSA Worker Configuration
# Copy this file to .env and fill in your values
#
# Usage:
#   1. cp .env.example .env
#   2. Edit .env with your values
#   3. docker-compose up -d
#   4. Scale workers: docker-compose up -d --scale worker=3

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# API key for authentication with the chess-compete server
# Get this from the server administrator
SPSA_API_KEY=your-api-key-here

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Base URL of the chess-compete API
# Default: https://chess-compete-production.up.railway.app
SPSA_API_URL=https://chess-compete-production.up.railway.app

# Worker name identifier (shows up in the workers dashboard)
# Use a unique name for each machine to track contributions
# Default: docker-worker
COMPUTER_NAME=docker-worker-1

# Number of parallel games per worker container
# Recommended: 2-4 per CPU core
# Higher values increase throughput but also memory usage
# Default: 4
CONCURRENCY=4

# =============================================================================
# RESOURCE LIMITS (configured in docker-compose.yml)
# =============================================================================
# The docker-compose.yml file includes resource limits per container:
#   - cpus: '4'     (max 4 CPU cores per container)
#   - memory: 4G    (max 4GB RAM per container)
#
# To adjust these, edit docker-compose.yml directly.

# =============================================================================
# SCALING
# =============================================================================
# To run multiple worker containers on the same machine:
#   docker-compose up -d --scale worker=3
#
# Each container will use its own CONCURRENCY setting, so with:
#   CONCURRENCY=4 and --scale worker=3
# You'll have 12 games running in parallel total.
